# Silver Layer Development

This folder contains the Silver-layer dbt transformation SQL used to clean,
standardize, combine, and enrich the grocery-sales source data.

## Structure

- `staging/` — source-level staging transformations
- `intermediate/` — intermediate sales transformations and enrichment

These files mirror the corresponding dbt models under the repository's
`models/` directory.
