# Gold Layer Development

This folder contains the Gold-layer dbt SQL models used to create the
analytics-ready dimensional model.

## Models

- `dim_date.sql`
- `dim_product.sql`
- `dim_store.sql`
- `fact_sales.sql`

These files mirror the corresponding dbt marts under the repository's
`models/marts/` directory.
